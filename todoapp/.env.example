# Root-level example (used mainly by docker-compose or future tooling)
# Currently docker-compose sets MONGO_URI for server directly; replicate here for consistency.
# Copy to server/.env or edit server/.env.example for backend-specific vars.

# Mongo connection string (server)
MONGO_URI=mongodb://mongo:27017/tododb
# Server port
PORT=5000
# React client API base override (optional). If unset defaults to http://localhost:5000/api
REACT_APP_API_URL=http://localhost:5000/api
